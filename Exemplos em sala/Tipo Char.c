#include <stdio.h>

int main(void) {
 
  char a = 'B';

 if (a == 'b') 
    printf("Value of a = a is ( %c )\n", a);
  else  
    printf("Value of a != a is ( %c )\n", a);
  return 0;

   
  
  return 0;
}